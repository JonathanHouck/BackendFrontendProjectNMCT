//
   Created by jonah on 12/1/2015.

div rentid {{rentid}}

div(ng-repeat="m in messages")
    p {{m.name}}
    p {{m.content}}

form(class="form-horizontal", name="chatForm", ng-submit="save()")
    div(class="form-group")
        textarea(cols="30", rows="2", name="note", ng-model="newMessage.content", placeholder="Jouw bericht", class="form-control", maxlength="120")
    div(class="form-group")
        input(type="submit" value="Stuur bericht" class="btn btn-primary")